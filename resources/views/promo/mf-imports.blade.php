<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>30 días Premium gratis — FAMER × MF Imports</title>
    <meta name="description" content="Exclusivo para clientes MF Imports: activa 30 días de Premium en FAMER sin costo. Código FAMER30.">
    <meta name="robots" content="noindex, nofollow">

    <!-- Open Graph -->
    <meta property="og:title" content="30 días Premium gratis — FAMER × MF Imports">
    <meta property="og:description" content="Tu restaurante merece ser descubierto por miles de clientes. Activa tu perfil Premium hoy.">
    <meta property="og:image" content="https://restaurantesmexicanosfamosos.com.mx/images/branding/famer55.png">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=poppins:400,500,600,700,800|playfair-display:400,700,900&display=swap" rel="stylesheet">

    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        :root {
            --gold: #D4AF37;
            --gold-light: #E8C67A;
            --gold-dark: #B08A1E;
            --base: #0B0B0B;
            --charcoal: #1A1A1A;
            --gray: #2A2A2A;
            --white: #F5F5F5;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--base);
            color: var(--white);
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }

        .page-wrap {
            max-width: 480px;
            margin: 0 auto;
            padding: 0 16px 48px;
        }

        /* ── HEADER ─────────────────────────────────────────── */
        .header {
            padding: 32px 0 24px;
            text-align: center;
            border-bottom: 1px solid var(--gray);
        }

        .logo {
            height: 56px;
            width: auto;
            margin-bottom: 20px;
        }

        .badge-partner {
            display: inline-block;
            background: var(--gray);
            border: 1px solid var(--gold);
            color: var(--gold);
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            padding: 4px 12px;
            border-radius: 100px;
            margin-bottom: 16px;
        }

        .headline {
            font-family: 'Playfair Display', serif;
            font-size: 30px;
            font-weight: 700;
            line-height: 1.2;
            color: var(--white);
            margin-bottom: 12px;
        }

        .headline span {
            background: linear-gradient(135deg, var(--gold-light) 0%, var(--gold) 50%, var(--gold-dark) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subheadline {
            font-size: 16px;
            color: #9CA3AF;
            line-height: 1.5;
        }

        /* ── VALUE PROPS ─────────────────────────────────────── */
        .section {
            padding: 28px 0;
            border-bottom: 1px solid var(--gray);
        }

        .section-label {
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--gold);
            text-align: center;
            margin-bottom: 20px;
        }

        .cards {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .card {
            background: var(--charcoal);
            border: 1px solid var(--gray);
            border-radius: 12px;
            padding: 18px 16px;
            display: flex;
            align-items: flex-start;
            gap: 14px;
        }

        .card-icon {
            font-size: 26px;
            flex-shrink: 0;
            margin-top: 2px;
        }

        .card-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--white);
            margin-bottom: 4px;
        }

        .card-text {
            font-size: 13px;
            color: #9CA3AF;
            line-height: 1.5;
        }

        /* ── PROMO CODE ──────────────────────────────────────── */
        .promo-section {
            padding: 28px 0;
            border-bottom: 1px solid var(--gray);
        }

        .promo-box {
            background: linear-gradient(135deg, #1C160A 0%, #231B0D 100%);
            border: 2px solid var(--gold);
            border-radius: 16px;
            padding: 28px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .promo-box::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(ellipse at center top, rgba(212,175,55,0.12) 0%, transparent 65%);
            pointer-events: none;
        }

        .promo-eyebrow {
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 12px;
        }

        .promo-code {
            font-family: 'Playfair Display', serif;
            font-size: 48px;
            font-weight: 900;
            letter-spacing: 4px;
            color: var(--gold);
            text-shadow: 0 0 24px rgba(212,175,55,0.35);
            margin-bottom: 12px;
            line-height: 1;
        }

        .promo-value {
            font-size: 15px;
            color: var(--white);
            font-weight: 500;
            margin-bottom: 8px;
        }

        .promo-expiry {
            font-size: 12px;
            color: #6B7280;
        }

        /* ── CTA BUTTON ──────────────────────────────────────── */
        .cta-section {
            padding: 28px 0;
            border-bottom: 1px solid var(--gray);
        }

        .cta-btn {
            display: block;
            width: 100%;
            min-height: 56px;
            background: linear-gradient(135deg, var(--gold) 0%, var(--gold-dark) 100%);
            color: #0B0B0B;
            font-family: 'Poppins', sans-serif;
            font-size: 17px;
            font-weight: 700;
            text-decoration: none;
            text-align: center;
            border-radius: 12px;
            padding: 14px 20px;
            line-height: 1.3;
            transition: opacity 0.2s, transform 0.1s;
            -webkit-tap-highlight-color: transparent;
        }

        .cta-btn:active {
            opacity: 0.85;
            transform: scale(0.98);
        }

        .cta-sub {
            text-align: center;
            font-size: 12px;
            color: #6B7280;
            margin-top: 10px;
        }

        /* ── TESTIMONIALS ────────────────────────────────────── */
        .testimonials-section {
            padding: 28px 0;
            border-bottom: 1px solid var(--gray);
        }

        .testimonial-card {
            background: var(--charcoal);
            border: 1px solid var(--gray);
            border-radius: 12px;
            padding: 20px 18px;
            margin-bottom: 12px;
            position: relative;
        }

        .testimonial-card:last-child {
            margin-bottom: 0;
        }

        .quote-mark {
            font-family: 'Playfair Display', serif;
            font-size: 48px;
            line-height: 1;
            color: var(--gold);
            opacity: 0.5;
            position: absolute;
            top: 10px;
            left: 14px;
        }

        .testimonial-text {
            font-size: 14px;
            color: var(--white);
            line-height: 1.6;
            padding-top: 24px;
            font-style: italic;
        }

        .testimonial-author {
            margin-top: 12px;
            font-size: 12px;
            font-weight: 600;
            color: var(--gold);
        }

        .star-row {
            color: var(--gold);
            font-size: 14px;
            margin-bottom: 4px;
        }

        /* ── FOOTER ──────────────────────────────────────────── */
        .footer {
            padding: 28px 0 0;
            text-align: center;
        }

        .footer-logo {
            height: 36px;
            width: auto;
            opacity: 0.6;
            margin-bottom: 12px;
        }

        .footer-text {
            font-size: 13px;
            color: #6B7280;
            line-height: 1.6;
        }

        .footer-text a {
            color: var(--gold);
            text-decoration: none;
        }

        /* ── DIVIDER ORNAMENT ────────────────────────────────── */
        .ornament {
            text-align: center;
            color: var(--gold);
            opacity: 0.4;
            font-size: 18px;
            letter-spacing: 8px;
            margin: 4px 0;
        }
    </style>
</head>
<body>
<div class="page-wrap">

    <!-- ── HEADER ────────────────────────────────────────────── -->
    <header class="header">
        <img src="/images/branding/famer55.png" alt="FAMER" class="logo">
        <div class="badge-partner">Exclusivo Clientes MF Imports</div>
        <h1 class="headline"><span>30 días Premium gratis</span><br>para clientes MF Imports</h1>
        <p class="subheadline">Tu restaurante merece ser descubierto por miles de clientes</p>
    </header>

    <!-- ── VALUE PROPS ───────────────────────────────────────── -->
    <section class="section">
        <div class="section-label">Lo que obtienes</div>
        <div class="cards">
            <div class="card">
                <div class="card-icon">🗺️</div>
                <div>
                    <div class="card-title">Más visibilidad</div>
                    <p class="card-text">Tu restaurante aparece en búsquedas de clientes que buscan comida mexicana en tu ciudad</p>
                </div>
            </div>
            <div class="card">
                <div class="card-icon">⭐</div>
                <div>
                    <div class="card-title">Perfil completo</div>
                    <p class="card-text">Fotos, menú, horarios, calificaciones — todo actualizado por ti</p>
                </div>
            </div>
            <div class="card">
                <div class="card-icon">📈</div>
                <div>
                    <div class="card-title">Atrae más clientes</div>
                    <p class="card-text">Los restaurantes con perfil Premium reciben <strong style="color: var(--gold);">3x más visitas</strong> que los básicos</p>
                </div>
            </div>
        </div>
    </section>

    <!-- ── PROMO CODE ─────────────────────────────────────────── -->
    <section class="promo-section">
        <div class="section-label">Tu código exclusivo</div>
        <div class="promo-box">
            <div class="promo-eyebrow">Código de activación</div>
            <div class="promo-code">FAMER30</div>
            <div class="promo-value">Activa 30 días de Premium sin costo — valor $29 USD</div>
            <div class="promo-expiry">⏳ Código válido por tiempo limitado</div>
        </div>
    </section>

    <!-- ── CTA ───────────────────────────────────────────────── -->
    <section class="cta-section">
        <a
            href="https://restaurantesmexicanosfamosos.com.mx/claim?promo=FAMER30"
            class="cta-btn"
        >
            Reclamar mi restaurante gratis →
        </a>
        <p class="cta-sub">Sin tarjeta de crédito — 30 días gratis, cancela cuando quieras</p>
    </section>

    <!-- ── TESTIMONIALS ──────────────────────────────────────── -->
    <section class="testimonials-section">
        <div class="section-label">Lo que dicen otros dueños</div>

        <div class="testimonial-card">
            <span class="quote-mark">"</span>
            <div class="star-row">★★★★★</div>
            <p class="testimonial-text">En 2 semanas ya teníamos 40 nuevas visitas desde FAMER. Nuestros clientes nos encontraron sin que nosotros hiciéramos nada. Vale cada centavo.</p>
            <div class="testimonial-author">El Rincón Tapatío — Houston, TX</div>
        </div>

        <div class="testimonial-card">
            <span class="quote-mark">"</span>
            <div class="star-row">★★★★★</div>
            <p class="testimonial-text">Nuestros clientes ahora nos encuentran en Google gracias al perfil Premium. Nunca pensé que sería tan fácil de configurar.</p>
            <div class="testimonial-author">La Paloma — Chicago, IL</div>
        </div>
    </section>

    <!-- ── FOOTER ─────────────────────────────────────────────── -->
    <footer class="footer">
        <img src="/images/branding/famer55.png" alt="FAMER" class="footer-logo">
        <p class="footer-text">
            ¿Preguntas? Escríbenos a<br>
            <a href="mailto:info@restaurantesmexicanosfamosos.com">info@restaurantesmexicanosfamosos.com</a>
        </p>
    </footer>

</div>
</body>
</html>
