<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="csrf-token" content="{{ csrf_token() }}">

    <!-- PWA Meta Tags -->
    <meta name="theme-color" content="#DC2626">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="FMR">
    <link rel="apple-touch-icon" href="/pwa-192x192.png">
    <link rel="icon" type="image/png" sizes="192x192" href="/pwa-192x192.png">

    <!-- SEO: Dynamic Title and Description -->
    <title>@yield('title', ($title ?? __('app.site_name')) . ' - ' . __('app.tagline'))</title>
    <meta name="description" content="@yield('meta_description', \App\Services\CountryContext::isMexico() ? 'Los 100 mejores restaurantes de Mexico basados en resenas de Google, Yelp, TripAdvisor y mas. Top 10 por ciudad y estado.' : __('app.tagline') . ' - Descubre los mejores restaurantes mexicanos autenticos')">

    <!-- SEO: Canonical URL -->
    <link rel="canonical" href="{{ url()->current() }}">

    <!-- SEO: Hreflang Tags for Bilingual Support -->
    <link rel="alternate" hreflang="en" href="https://famousmexicanrestaurants.com{{ request()->path() === '/' ? '' : '/' . request()->path() }}" />
    <link rel="alternate" hreflang="es" href="https://restaurantesmexicanosfamosos.com{{ request()->path() === '/' ? '' : '/' . request()->path() }}" />
    <link rel="alternate" hreflang="x-default" href="https://famousmexicanrestaurants.com{{ request()->path() === '/' ? '' : '/' . request()->path() }}" />

    <!-- Open Graph Locale -->
    <meta property="og:locale" content="{{ app()->getLocale() === 'en' ? 'en_US' : 'es_US' }}" />
    <meta property="og:site_name" content="{{ __('app.site_name') }}" />

    <!-- Dynamic Meta Tags (Open Graph, Twitter Cards) -->
    @stack('meta')

    <!-- Schema.org WebSite -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "{{ __('app.site_name') }}",
        "url": "{{ config('app.url') }}",
        "description": "{{ __('app.tagline') }}",
        "inLanguage": ["es", "en"],
        "potentialAction": {
            "@type": "SearchAction",
            "target": "{{ config('app.url') }}/restaurants?search={search_term_string}",
            "query-input": "required name=search_term_string"
        }
    }
    </script>
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "Famous Mexican Restaurants",
        "url": "https://restaurantesmexicanosfamosos.com.mx",
        "logo": "https://restaurantesmexicanosfamosos.com.mx/pwa-512x512.png",
        "description": "Directorio de los mejores restaurantes mexicanos en México y USA",
        "sameAs": [
            "https://famousmexicanrestaurants.com"
        ]
    }
    </script>

    <!-- Fonts - Poppins para modernidad -->
    <link rel="preconnect" href="https://fonts.bunny.net">
    <link href="https://fonts.bunny.net/css?family=poppins:400,500,600,700,800|playfair-display:700,900&display=fallback" rel="stylesheet" />

    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer','{{ app()->getLocale() === "en" ? "GTM-NLKPXHKM" : "GTM-M53NLTND" }}');</script>
    <!-- End Google Tag Manager -->

    <!-- Scripts -->
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles

    <style>
/* Critical: Prevent CLS from font loading */
body, .font-sans { font-family: Poppins, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }
.font-display { font-family: "Playfair Display", Georgia, "Times New Roman", serif; }
/* Reserve space for nav */
nav { min-height: 80px; }
/* Aspect ratio for images */
.aspect-video { aspect-ratio: 16/9; }
        body {
            font-family: 'Poppins', sans-serif;
        }
        .font-display {
            font-family: 'Playfair Display', serif;
        }

        /* Pattern mexicano sutil */
        .mexican-pattern {
            background-image:
                repeating-linear-gradient(45deg, transparent, transparent 10px, rgba(220, 38, 38, 0.02) 10px, rgba(220, 38, 38, 0.02) 20px);
        }

        /* Efecto de brillo en botones */
        .btn-shine {
            position: relative;
            overflow: hidden;
        }
        .btn-shine::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.5s;
        }
        .btn-shine:hover::before {
            left: 100%;
        }

        /* iOS Install Banner Styles */
        .ios-install-banner {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            z-index: 9999;
            transform: translateY(100%);
            transition: transform 0.3s ease-out;
            padding: 0 1rem 1rem;
        }

        .ios-install-banner.show {
            transform: translateY(0);
        }

        .ios-banner-content {
            background: linear-gradient(135deg, #ffffff 0%, #f9fafb 100%);
            border-radius: 1rem 1rem 0 0;
            padding: 1.25rem;
            box-shadow: 0 -4px 24px rgba(0, 0, 0, 0.15);
            border-top: 3px solid #DC2626;
            position: relative;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .ios-banner-close {
            position: absolute;
            top: 0.75rem;
            right: 0.75rem;
            background: none;
            border: none;
            color: #6b7280;
            cursor: pointer;
            padding: 0.25rem;
            border-radius: 0.375rem;
            transition: all 0.2s;
        }

        .ios-banner-close:hover {
            background: #f3f4f6;
            color: #1f2937;
        }

        .ios-banner-icon img {
            width: 48px;
            height: 48px;
            border-radius: 0.75rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .ios-banner-text {
            flex: 1;
        }

        .ios-banner-text h3 {
            font-size: 1rem;
            font-weight: 700;
            color: #1f2937;
            margin: 0 0 0.5rem 0;
        }

        .ios-banner-steps {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
            font-size: 0.875rem;
            color: #4b5563;
        }

        .ios-step {
            display: flex;
            align-items: center;
            gap: 0.375rem;
        }

        .ios-share-icon {
            color: #3b82f6;
            flex-shrink: 0;
        }

        @media (min-width: 640px) {
            .ios-install-banner {
                padding: 0 2rem 2rem;
            }

            .ios-banner-content {
                max-width: 32rem;
                margin: 0 auto;
                border-radius: 1rem;
            }
        }
    </style>
</head>
<body class="bg-gradient-to-br from-orange-50 via-white to-emerald-50 antialiased">
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id={{ app()->getLocale() === 'en' ? 'GTM-NLKPXHKM' : 'GTM-M53NLTND' }}"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->

    <!-- Decorative Top Border -->
    <div class="h-2 bg-gradient-to-r from-emerald-600 via-white to-red-600"></div>

    <!-- Navigation -->
    <nav class="bg-white/95 backdrop-blur-sm shadow-lg sticky top-0 z-50 border-b-4 border-red-600">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20">
                <div class="flex items-center">
                    <a href="/" class="flex items-center group">
                        <!-- Logo Mexicano Estilizado -->
                        <div class="relative">
                            <div class="absolute inset-0 bg-gradient-to-br from-emerald-500 to-red-600 rounded-xl blur-md opacity-50 group-hover:opacity-75 transition-opacity"></div>
                            <div class="relative bg-gradient-to-br from-emerald-600 to-red-600 p-2 rounded-xl">
                                <svg class="h-8 w-8 text-white" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-5.5-2.5l7.51-3.49L17.5 6.5 9.99 9.99 6.5 17.5zm5.5-6.6c.61 0 1.1.49 1.1 1.1s-.49 1.1-1.1 1.1-1.1-.49-1.1-1.1.49-1.1 1.1-1.1z"/>
                                </svg>
                            </div>
                        </div>
                        <div class="ml-3">
                            <span class="text-2xl font-display font-bold bg-gradient-to-r from-emerald-700 via-red-600 to-orange-600 bg-clip-text text-transparent">
                                {{ app()->getLocale() === 'en' ? 'Restaurants' : 'Restaurantes' }}
                            </span>
                            <div class="text-xs font-semibold text-gray-600 tracking-wider">
                                {{ $isMexico ? 'MEXICANOS FAMOSOS' : (app()->getLocale() === 'en' ? 'MEXICAN IN USA' : 'MEXICANOS EN USA') }}
                            </div>
                        </div>
                    </a>
                </div>

                <!-- Desktop Navigation -->
                <div class="hidden lg:flex items-center space-x-2">
                    <a href="/" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                        {{ __('app.home') }}
                    </a>
                    <a href="/restaurantes" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                        {{ __('app.restaurants') }}
                    </a>
                    <a href="/sugerir" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                        {{ __('app.suggest') }}
                    </a>
                    <a href="/for-owners" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                        {{ app()->getLocale() === 'en' ? 'For Owners' : 'Para Dueños' }}
                    </a>
                    <a href="/guia" class="text-gray-700 hover:text-emerald-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-emerald-50 transition-all flex items-center gap-1">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                        </svg>
                        {{ app()->getLocale() === 'en' ? 'City Guide' : 'Guía' }}
                    </a>

                    <!-- Language Switcher (Desktop) -->
                    <x-language-switcher />

                    <!-- Claim Restaurant Button - Destacado -->
                    <a href="/claim" class="btn-shine relative bg-gradient-to-r from-green-600 to-emerald-600 text-white px-6 py-2.5 rounded-xl text-sm font-semibold hover:shadow-xl hover:scale-105 transition-all duration-300 flex items-center gap-2">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        {{ app()->getLocale() === 'en' ? 'Claim Restaurant' : 'Reclamar' }}
                    </a>

                    <!-- Auth Links (Desktop) -->
                    @auth
                        <a href="/my-favorites" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all flex items-center gap-1">
                            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd"></path>
                            </svg>
                            {{ app()->getLocale() === 'en' ? 'My Favorites' : 'Mis Favoritos' }}
                        </a>
                        @if(auth()->user()->restaurants()->exists())
                            <a href="/owner" class="text-emerald-600 hover:text-emerald-700 px-4 py-2 rounded-lg text-sm font-semibold hover:bg-emerald-50 transition-all flex items-center gap-1">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                                </svg>
                                {{ app()->getLocale() === 'en' ? 'My Restaurant' : 'Mi Restaurante' }}
                            </a>
                        @endif
                        @if(auth()->user()->role === 'admin')
                            <a href="/admin" class="text-purple-600 hover:text-purple-700 px-4 py-2 rounded-lg text-sm font-semibold hover:bg-purple-50 transition-all">
                                Admin
                            </a>
                        @endif
                        <form method="POST" action="{{ route('logout') }}" class="inline">
                            @csrf
                            <button type="submit" class="text-gray-600 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                                {{ app()->getLocale() === 'en' ? 'Logout' : 'Salir' }}
                            </button>
                        </form>
                    @else
                        <a href="/login" class="text-gray-700 hover:text-red-600 px-4 py-2 rounded-lg text-sm font-medium hover:bg-red-50 transition-all">
                            Login
                        </a>
                        <a href="/register" class="btn-shine relative bg-gradient-to-r from-red-600 to-orange-600 text-white px-6 py-2.5 rounded-xl text-sm font-semibold hover:shadow-xl hover:scale-105 transition-all duration-300">
                            Register
                        </a>
                    @endauth

                </div>

                <!-- Mobile menu button (Only visible on mobile) -->
                <div class="flex lg:hidden items-center space-x-2">
                    <!-- Language Switcher (Mobile) -->
                    <x-language-switcher />

                    <!-- Hamburger Button -->
                    <button id="mobile-menu-btn" type="button" class="inline-flex items-center justify-center p-2 rounded-lg text-gray-700 hover:text-red-600 hover:bg-red-50 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-red-500">
                        <span class="sr-only">{{ app()->getLocale() === 'en' ? 'Open main menu' : 'Abrir menú principal' }}</span>
                        <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile menu -->
        <div id="mobile-menu" class="hidden lg:hidden border-t border-gray-200">
            <div class="px-2 pt-2 pb-3 space-y-1">
                <a href="/" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                    {{ __('app.home') }}
                </a>
                <a href="/restaurantes" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                    {{ __('app.restaurants') }}
                </a>
                <a href="/sugerir" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                    {{ __('app.suggest') }}
                </a>
                <a href="/for-owners" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                    {{ app()->getLocale() === 'en' ? 'For Owners' : 'Para Dueños' }}
                </a>
                <a href="/guia" class="block text-gray-700 hover:text-emerald-600 hover:bg-emerald-50 px-3 py-2 rounded-lg text-base font-medium transition-all flex items-center gap-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    </svg>
                    {{ app()->getLocale() === 'en' ? 'City Guide' : 'Guía por Ciudad' }}
                </a>
                <a href="/claim" class="block bg-gradient-to-r from-green-600 to-emerald-600 text-white px-3 py-2 rounded-lg text-base font-semibold transition-all">
                    {{ app()->getLocale() === 'en' ? 'Claim Restaurant' : 'Reclamar' }}
                </a>
                @auth
                    <a href="/my-favorites" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                        {{ app()->getLocale() === 'en' ? 'My Favorites' : 'Mis Favoritos' }}
                    </a>
                    @if(auth()->user()->restaurants()->exists())
                        <a href="/owner" class="block text-emerald-600 hover:text-emerald-700 hover:bg-emerald-50 px-3 py-2 rounded-lg text-base font-semibold transition-all">
                            {{ app()->getLocale() === 'en' ? 'My Restaurant' : 'Mi Restaurante' }}
                        </a>
                    @endif
                    @if(auth()->user()->role === 'admin')
                        <a href="/admin" class="block text-purple-600 hover:text-purple-700 hover:bg-purple-50 px-3 py-2 rounded-lg text-base font-semibold transition-all">
                            Admin
                        </a>
                    @endif
                    <form method="POST" action="{{ route('logout') }}" class="block">
                        @csrf
                        <button type="submit" class="w-full text-left text-gray-600 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                            {{ app()->getLocale() === 'en' ? 'Logout' : 'Salir' }}
                        </button>
                    </form>
                @else
                    <a href="/login" class="block text-gray-700 hover:text-red-600 hover:bg-red-50 px-3 py-2 rounded-lg text-base font-medium transition-all">
                        Login
                    </a>
                    <a href="/register" class="block bg-gradient-to-r from-red-600 to-orange-600 text-white px-3 py-2 rounded-lg text-base font-semibold transition-all">
                        Register
                    </a>
                @endauth

            </div>
        </div>
    </nav>

    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-btn')?.addEventListener('click', function() {
            const mobileMenu = document.getElementById('mobile-menu');
            mobileMenu.classList.toggle('hidden');
        });
    </script>

    <!-- PWA Install Button (Hidden by default, shown by JavaScript when installable) -->
    <div id="pwa-install-btn" class="hidden fixed bottom-6 right-6 z-50 animate-bounce">
        <button onclick="installPWA()" class="bg-gradient-to-r from-emerald-600 to-green-600 text-white px-6 py-4 rounded-2xl shadow-2xl hover:shadow-3xl hover:scale-105 transition-all duration-300 flex items-center gap-3 font-semibold">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
            </svg>
            <span>{{ app()->getLocale() === 'en' ? 'Install App' : 'Instalar App' }}</span>
        </button>
    </div>

    <!-- Main Content -->
    <main class="min-h-screen">
        @isset($slot)
            {{ $slot }}
        @else
            @yield('content')
        @endisset
    </main>

    <!-- Cross-Promotion Section -->
    {{-- <x-cross-promotion current-site="restaurantes" /> --}}

    <!-- Footer -->
    <footer class="relative bg-gradient-to-br from-gray-900 via-gray-800 to-emerald-900 text-white mt-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <!-- Borde decorativo superior -->
            <div class="h-1 bg-gradient-to-r from-emerald-500 via-white to-red-600 rounded-full mb-8"></div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
                <!-- About -->
                <div>
                    <h3 class="text-xl font-bold mb-4 flex items-center">
                        <span class="bg-gradient-to-r from-emerald-400 to-red-500 bg-clip-text text-transparent">Restaurantes Mexicanos</span>
                    </h3>
                    <p class="text-white text-base leading-relaxed opacity-90">
                        El directorio más completo de restaurantes mexicanos auténticos en Estados Unidos. Encuentra los mejores sabores de México cerca de ti. ¡Buen provecho! 🌮
                    </p>
                </div>

                <!-- Quick Links -->
                <div>
                    <h3 class="text-lg font-bold mb-4 text-emerald-400">{{ app()->getLocale() === 'en' ? 'Quick Links' : 'Enlaces Rápidos' }}</h3>
                    <ul class="space-y-3 text-base">
                        <li><a href="/" class="text-white hover:text-emerald-400 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'Home' : 'Inicio' }}</a></li>
                        <li><a href="/restaurantes" class="text-white hover:text-emerald-400 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'All Restaurants' : 'Todos los Restaurantes' }}</a></li>
                        <li><a href="/guia" class="text-white hover:text-emerald-400 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'City Guides' : 'Guía por Ciudad' }}</a></li>
                        <li><a href="/sugerir" class="text-white hover:text-emerald-400 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'Suggest Restaurant' : 'Sugerir Restaurante' }}</a></li>
                        <li><a href="/preguntas-frecuentes" class="text-white hover:text-emerald-400 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'FAQ' : 'Preguntas Frecuentes' }}</a></li>
                        <li><a href="/for-owners" class="text-white hover:text-yellow-300 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'For Owners' : 'Para Dueños' }}</a></li>
                        <li><a href="/claim" class="text-white hover:text-yellow-300 transition-colors font-medium opacity-90">→ {{ app()->getLocale() === 'en' ? 'Claim Your Restaurant' : 'Reclama tu Restaurante' }}</a></li>
                    </ul>
                </div>

                <!-- Businesses -->
                <div>
                    <h3 class="text-lg font-bold mb-4 text-orange-400">Nuestros Negocios</h3>
                    <ul class="space-y-3 text-base">
                        <li><a href="https://mueblesmexicanos.com" class="text-white hover:text-orange-400 transition-colors font-medium opacity-90" target="_blank">→ Muebles Mexicanos</a></li>
                        <li><a href="#" class="text-white hover:text-orange-400 transition-colors font-medium opacity-90">→ RefriMex Paletería</a></li>
                        <li><a href="#" class="text-white hover:text-orange-400 transition-colors font-medium opacity-90">→ DecorarMex</a></li>
                        <li><a href="#" class="text-white hover:text-orange-400 transition-colors font-medium opacity-90">→ TorMex</a></li>
                        <li><a href="#" class="text-white hover:text-orange-400 transition-colors font-medium opacity-90">→ Trailer Elite</a></li>
                    </ul>
                </div>
            </div>

            <!-- Bottom Bar -->
            <div class="border-t border-gray-500 pt-8 text-center">
                <p class="text-white text-base opacity-90">
                    &copy; {{ date('Y') }} <span class="font-bold">Restaurantes Mexicanos Famosos</span>.
                    Hecho con ❤️ para los amantes de la auténtica comida mexicana.
                </p>
            </div>

            <!-- Legal Disclaimer - Data Sources -->
            <div class="mt-6 pt-4 border-t border-gray-600">
                <p class="text-gray-400 text-xs text-center leading-relaxed max-w-4xl mx-auto">
                    <strong>Aviso Legal:</strong> La información de restaurantes mostrada en este sitio es recopilada de fuentes públicas incluyendo
                    <a href="https://www.yelp.com" target="_blank" rel="noopener" class="text-red-400 hover:text-red-300">Yelp</a> y
                    <a href="https://www.google.com/maps" target="_blank" rel="noopener" class="text-blue-400 hover:text-blue-300">Google</a>
                    a través de sus APIs oficiales. Los datos se actualizan periódicamente para garantizar precisión.
                    Yelp® y el logo de Yelp son marcas registradas de Yelp Inc. Google® y Google Maps® son marcas registradas de Google LLC.
                    Los propietarios de restaurantes pueden <a href="/claim" class="text-emerald-400 hover:text-emerald-300 underline">reclamar y verificar su perfil</a>
                    para gestionar su información directamente.
                </p>
                <p class="text-gray-500 text-xs text-center mt-2">
                    <a href="/privacy" class="hover:text-gray-400">Política de Privacidad</a> ·
                    <a href="/terms" class="hover:text-gray-400">Términos de Uso</a> ·
                    <a href="/contact" class="hover:text-gray-400">Contacto</a> ·
                    <a href="/preguntas-frecuentes" class="hover:text-gray-400">FAQ</a>
                </p>

                <!-- MF Group Cross-linking -->
                <div class="border-t border-gray-700 mt-6">
                    <x-mfgroup-footer />
                </div>
            </div>
        </div>
    </footer>

    <!-- Cart Component -->
    <livewire:cart />
    @livewireScripts

    <!-- Dynamic Scripts (Schema.org, Social Sharing) -->
    @stack('scripts')
    <!-- Chatwoot Live Chat -->
    <script>
      (function(d,t) {
        var BASE_URL="https://chat.mefimports.com";
        var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
        g.src=BASE_URL+"/packs/js/sdk.js";
        g.defer = true;
        s.parentNode.insertBefore(g,s);
        g.onload=function(){
          window.chatwootSDK.run({
            websiteToken: 'YfQ6iFNku9dYWic9PjXfaQ5J',
            baseUrl: BASE_URL
          });
          
          // Auto-open chat after 5 seconds
          window.addEventListener('chatwoot:ready', function() {
            setTimeout(function() {
              if (!sessionStorage.getItem('chatwoot_auto_opened')) {
                window.$chatwoot.toggle('open');
                sessionStorage.setItem('chatwoot_auto_opened', 'true');
              }
            }, 5000);
          })
        }
      })(document,"script");
    </script>
</body>
</html>
