<div>
    <!-- Open Graph Tags for Social Sharing -->
    @push('meta')
        <x-open-graph
            :title="$restaurant->name . ' - ' . __('app.site_name')"
            :description="$restaurant->description ?: __('app.tagline')"
            :image="$restaurant->image ? asset('storage/' . $restaurant->image) : ($restaurant->getFirstMediaUrl('images') ?: asset('images/restaurant-placeholder.jpg'))"
            :url="url()->current()"
            type="restaurant.restaurant"
        />
    @endpush

    <!-- Schema.org Structured Data for SEO -->
    @push('scripts')
        <x-schema-org
            type="Restaurant"
            :data="[
                'name' => $restaurant->name,
                'image' => $restaurant->image ? asset('storage/' . $restaurant->image) : ($restaurant->getFirstMediaUrl('images') ?: asset('images/restaurant-placeholder.jpg')),
                'address' => [
                    '@type' => 'PostalAddress',
                    'streetAddress' => $restaurant->address,
                    'addressLocality' => $restaurant->city,
                    'addressRegion' => $restaurant->state->code,
                    'postalCode' => $restaurant->zip_code,
                    'addressCountry' => 'US',
                ],
                'telephone' => $restaurant->phone,
                'url' => $restaurant->website,
                'servesCuisine' => 'Mexican',
                'priceRange' => $restaurant->price_range ?? '$$',
                'aggregateRating' => [
                    '@type' => 'AggregateRating',
                    'ratingValue' => $restaurant->average_rating,
                    'reviewCount' => $restaurant->total_reviews,
                    'bestRating' => 5,
                    'worstRating' => 1,
                ],
            ]"
        />
    @endpush

    <!-- Header with Image -->
    <div class="bg-gray-900 h-64 md:h-96 relative">
        @if($restaurant->image)
            <img src="{{ asset('storage/' . $restaurant->image) }}"
                 alt="{{ $restaurant->name }}"
                 class="w-full h-full object-cover opacity-80">
        @elseif($restaurant->getFirstMediaUrl('images'))
            <img src="{{ $restaurant->getFirstMediaUrl('images') }}"
                 alt="{{ $restaurant->name }}"
                 class="w-full h-full object-cover opacity-80">
        @else
            <div class="w-full h-full bg-gradient-to-br from-red-600 to-orange-600"></div>
        @endif
        <div class="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"></div>
    </div>

    <!-- Restaurant Info -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 -mt-20 relative z-10">
        <div class="bg-white rounded-lg shadow-xl p-8">
            <div class="lg:flex lg:items-center lg:justify-between">
                <div class="flex-1 min-w-0">
                    <div class="flex items-center mb-4">
                        <span class="bg-red-100 text-red-800 text-sm font-semibold px-3 py-1 rounded-full">
                            {{ $restaurant->category->name }}
                        </span>
                        @if($restaurant->is_featured)
                            <span class="ml-2 bg-yellow-100 text-yellow-800 text-sm font-semibold px-3 py-1 rounded-full">
                                ⭐ Destacado
                            </span>
                        @endif
                    </div>
                    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-2">
                        {{ $restaurant->name }}
                    </h1>
                    <div class="flex items-center text-gray-600 mb-4">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <span>{{ $restaurant->city }}, {{ $restaurant->state->name }}</span>
                    </div>
                    <div class="flex items-center">
                        <div class="flex text-yellow-400 mr-2">
                            @for($i = 0; $i < 5; $i++)
                                <svg class="w-5 h-5 {{ $i < floor($restaurant->average_rating) ? 'fill-current' : 'fill-gray-300' }}" viewBox="0 0 20 20">
                                    <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                                </svg>
                            @endfor
                        </div>
                        <span class="text-gray-700 font-semibold">{{ number_format($restaurant->average_rating, 1) }}</span>
                        <span class="text-gray-500 ml-2">({{ $restaurant->total_reviews }} reviews)</span>
                    </div>
                </div>
                <div class="mt-6 lg:mt-0 lg:ml-6 flex-shrink-0">
                    <a href="tel:{{ $restaurant->phone }}" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-red-600 hover:bg-red-700">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                        </svg>
                        Llamar Ahora
                    </a>
                </div>
            </div>

            <!-- Description -->
            @if($restaurant->description)
            <div class="mt-8 prose max-w-none">
                <p class="text-gray-700 text-lg">{{ $restaurant->description }}</p>
            </div>
            @endif
        </div>

        <!-- Tab Navigation -->
        <div class="mt-8 bg-white rounded-lg shadow">
            <div class="border-b border-gray-200">
                <nav class="-mb-px flex space-x-8 px-6" aria-label="Tabs">
                    <button
                        wire:click="switchTab('info')"
                        class="{{ $activeTab === 'info' ? 'border-red-500 text-red-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' }} whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors"
                    >
                        <svg class="w-5 h-5 inline-block mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        Información
                    </button>

                    @if($menuItems->count() > 0)
                    <button
                        wire:click="switchTab('menu')"
                        class="{{ $activeTab === 'menu' ? 'border-red-500 text-red-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' }} whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors"
                    >
                        <svg class="w-5 h-5 inline-block mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                        </svg>
                        Menú
                        <span class="ml-1 text-xs bg-red-100 text-red-600 py-0.5 px-2 rounded-full">{{ $menuItems->count() }}</span>
                    </button>
                    @endif

                    <button
                        wire:click="switchTab('reviews')"
                        class="{{ $activeTab === 'reviews' ? 'border-red-500 text-red-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' }} whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors"
                    >
                        <svg class="w-5 h-5 inline-block mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
                        </svg>
                        Resenas
                        <span class="ml-1 text-xs bg-yellow-100 text-yellow-600 py-0.5 px-2 rounded-full">{{ $restaurant->total_reviews }}</span>
                    </button>

                    <button
                        wire:click="switchTab('photos')"
                        class="{{ $activeTab === 'photos' ? 'border-red-500 text-red-600' : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300' }} whitespace-nowrap py-4 px-1 border-b-2 font-medium text-sm transition-colors"
                    >
                        <svg class="w-5 h-5 inline-block mr-2 -mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                        </svg>
                        Fotos
                        <span class="ml-1 text-xs bg-blue-100 text-blue-600 py-0.5 px-2 rounded-full">{{ $restaurant->approvedPhotos()->count() }}</span>
                    </button>
                </nav>
            </div>
        </div>

        <!-- Tab Content -->
        <div class="mt-8">
            <!-- Info Tab -->
            @if($activeTab === 'info')
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Left Column - Contact & Location -->
            <div class="lg:col-span-2 space-y-6">
                <!-- Contact Information -->
                <div class="bg-white rounded-lg shadow p-6">
                    <h2 class="text-2xl font-bold text-gray-900 mb-6">Información de Contacto</h2>
                    <div class="space-y-4">
                        <div class="flex items-start">
                            <svg class="w-6 h-6 text-red-600 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                            </svg>
                            <div>
                                <p class="font-semibold text-gray-900">Dirección</p>
                                <p class="text-gray-600">{{ $restaurant->address }}</p>
                                <p class="text-gray-600">{{ $restaurant->city }}, {{ $restaurant->state->code }} {{ $restaurant->zip_code }}</p>
                            </div>
                        </div>

                        @if($restaurant->phone)
                        <div class="flex items-center">
                            <svg class="w-6 h-6 text-red-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                            </svg>
                            <div>
                                <p class="font-semibold text-gray-900">Teléfono</p>
                                <a href="tel:{{ $restaurant->phone }}" class="text-red-600 hover:text-red-700">{{ $restaurant->phone }}</a>
                            </div>
                        </div>
                        @endif

                        @if($restaurant->email)
                        <div class="flex items-center">
                            <svg class="w-6 h-6 text-red-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                            </svg>
                            <div>
                                <p class="font-semibold text-gray-900">Email</p>
                                <a href="mailto:{{ $restaurant->email }}" class="text-red-600 hover:text-red-700">{{ $restaurant->email }}</a>
                            </div>
                        </div>
                        @endif

                        @if($restaurant->website)
                        <div class="flex items-center">
                            <svg class="w-6 h-6 text-red-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"/>
                            </svg>
                            <div>
                                <p class="font-semibold text-gray-900">Sitio Web</p>
                                <a href="{{ $restaurant->website }}" target="_blank" class="text-red-600 hover:text-red-700">Visitar sitio web</a>
                            </div>
                        </div>
                        @endif
                    </div>
                </div>

                <!-- Google Maps Integration -->
                <div class="bg-white rounded-lg shadow p-6">
                    <h2 class="text-2xl font-bold text-gray-900 mb-4">{{ __('app.location') }}</h2>
                    <x-google-map
                        :name="$restaurant->name"
                        :address="$restaurant->address . ', ' . $restaurant->city . ', ' . $restaurant->state->code . ' ' . $restaurant->zip_code"
                        height="400"
                        zoom="15"
                    />
                </div>
            </div>

            <!-- Right Column - Additional Info -->
            <div class="space-y-6">
                <!-- FAMER Awards Voting -->

                <!-- Live Statistics -->

                <!-- Reservations -->
                @livewire('reservation-form', ['restaurant' => $restaurant])

                <!-- Share -->
                <div class="bg-white rounded-lg shadow p-6">
                    <h3 class="text-lg font-bold text-gray-900 mb-4">{{ __('app.share') }}</h3>
                    <x-social-share
                        :url="url()->current()"
                        :title="$restaurant->name"
                        :description="$restaurant->description ?: __('app.tagline')"
                        layout="horizontal"
                    />
                </div>

                <!-- Report Issue -->
                <div class="bg-gray-50 rounded-lg border-2 border-dashed border-gray-300 p-6 text-center">
                    <p class="text-gray-600 mb-3">¿Algo incorrecto?</p>
                    @livewire('report-restaurant', ['restaurant' => $restaurant])
                </div>

                <!-- Ad Space -->
                @livewire('advertisement-banner', ['placement' => 'sidebar', 'stateId' => $restaurant->state_id])
            </div>
            </div>
            @endif

            <!-- Menu Tab -->
            @if($activeTab === 'menu' && $menuItems->count() > 0)
                @include('livewire.partials.restaurant-menu')
            @endif

            <!-- Reviews Tab -->
            @if($activeTab === 'reviews')
            <div class="space-y-8">
                <!-- Write Review Form -->
                <div id="write-review">
                    @livewire('write-review', ['restaurant' => $restaurant])
                </div>

                <!-- Reviews List -->
                <div>
                    @livewire('review-list', ['restaurant' => $restaurant])
                </div>
            </div>
            @endif

            <!-- Photos Tab -->
            @if($activeTab === 'photos')
            <div>
                @livewire('photo-gallery', ['restaurant' => $restaurant])
            </div>
            @endif
        </div>
    </div>

    <!-- Menu Item Modal -->
    @if($showMenuItemModal && $selectedMenuItem)
        @include('livewire.partials.menu-item-modal')
    @endif
</div>
